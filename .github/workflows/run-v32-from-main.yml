name: run-v33-from-main
on:
  push:
    branches: [main]
    paths: ['.github/workflows/run-v32-from-main.yml']
permissions:
  contents: read
  issues: write
jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/v33-serial-surplus-land
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - run: pip install -q kaggle-environments
      - name: Compile candidate
        run: |
          python -m py_compile agents/v19_livestock_compound.py agents/v19_2_early_scale8.py agents/v33_serial_surplus_land.py
      - name: Benchmark V19.2 baseline isolated
        run: |
          mkdir -p artifacts
          python - <<'PY'
          import json
          from pathlib import Path
          from kaggle_environments import make
          import agents.v19_2_early_scale8 as a
          rows=[]
          for seat in (0,1):
            for i in range(16):
              a.reset_state(); seed=333000+i
              env=make('kaggriculture',configuration={'seed':seed},debug=True)
              env.run([a.agent,'starter'] if seat==0 else ['starter',a.agent])
              st=env.state[seat]; tele=a.get_telemetry(clear=True) or []
              dm={int(r.get('day',-1)):float(r.get('money',0) or 0) for r in tele}
              rows.append({'seat':seat,'seed':seed,'reward':float(st.reward or 0),'status':str(st.status),'d12':dm.get(12,0),'d20':dm.get(20,0),'d25':dm.get(25,0),'max_land':max([int(r.get('unlocked_count',1) or 1) for r in tele] or [1])})
          Path('artifacts/v19_2.json').write_text(json.dumps(rows,indent=2))
          PY
      - name: Benchmark V33 isolated
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          from kaggle_environments import make
          import agents.v33_serial_surplus_land as a
          rows=[]
          for seat in (0,1):
            for i in range(16):
              a.reset_state(); seed=333000+i
              env=make('kaggriculture',configuration={'seed':seed},debug=True)
              env.run([a.agent,'starter'] if seat==0 else ['starter',a.agent])
              st=env.state[seat]; tele=a.get_telemetry(clear=True) or []
              dm={int(r.get('day',-1)):float(r.get('money',0) or 0) for r in tele}
              rows.append({'seat':seat,'seed':seed,'reward':float(st.reward or 0),'status':str(st.status),'d12':dm.get(12,0),'d20':dm.get(20,0),'d25':dm.get(25,0),'max_land':max([int(r.get('unlocked_count',1) or 1) for r in tele] or [1]),'serial_days':[int(r.get('day',-1)) for r in tele if r.get('serial_expand')]})
          Path('artifacts/v33.json').write_text(json.dumps(rows,indent=2))
          PY
      - name: Compare paired results
        run: |
          python - <<'PY'
          import json,statistics
          from pathlib import Path
          BAD={'ERROR','INVALID','TIMEOUT'}
          b=json.loads(Path('artifacts/v19_2.json').read_text()); c=json.loads(Path('artifacts/v33.json').read_text())
          key=lambda r:(r['seat'],r['seed']); bm={key(r):r for r in b}; cm={key(r):r for r in c}
          def summ(rows):
            rewards=[r['reward'] for r in rows]
            sr=sorted(rewards)
            return {'games':len(rows),'mean':statistics.fmean(rewards),'median':statistics.median(rewards),'min_reward':min(rewards),'p10_reward':sr[max(0,int(len(sr)*0.10)-1)],'p25_reward':statistics.median(sr[:max(1,len(sr)//2)]),'max_reward':max(rewards),'median_d12':statistics.median(r['d12'] for r in rows),'median_d20':statistics.median(r['d20'] for r in rows),'median_d25':statistics.median(r['d25'] for r in rows),'invalid':sum(r['status'] in BAD for r in rows),'two_land_games':sum(r['max_land']>=2 for r in rows),'three_land_games':sum(r['max_land']>=3 for r in rows),'four_land_games':sum(r['max_land']>=4 for r in rows)}
          sb,sc=summ(b),summ(c); ds=[cm[k]['reward']-bm[k]['reward'] for k in bm]
          paired={'wins':sum(d>0 for d in ds),'losses':sum(d<0 for d in ds),'ties':sum(d==0 for d in ds),'mean_delta':statistics.fmean(ds),'median_delta':statistics.median(ds)}
          ready150=(sc['invalid']==0 and sc['games']>=24 and sc['median']>=150000 and sc['min_reward']>=100000)
          promote=(sc['invalid']==0 and sc['median']>sb['median'] and sc['mean']>sb['mean'] and paired['wins']>=20 and paired['mean_delta']>0)
          report={'version':'33.0.0-alpha1','mechanism':'serial surplus-funded third/fourth land only','baseline_v19_2':sb,'candidate_v33':sc,'paired':paired,'promotion_pass':promote,'ready_150k':ready150}
          Path('artifacts/v33_report.json').write_text(json.dumps(report,indent=2)); print(json.dumps(report,indent=2))
          PY
      - name: Post report to issue 16
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ -f artifacts/v33_report.json ]; then
            { echo '## V33 isolated serial-surplus-land benchmark'; echo; echo '```json'; cat artifacts/v33_report.json; echo '```'; } > /tmp/v33.md
          else
            echo '## V33 benchmark failed before report generation' > /tmp/v33.md
          fi
          gh issue comment 16 --repo "$GITHUB_REPOSITORY" --body-file /tmp/v33.md
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: v33-isolated-serial-surplus-land
          path: artifacts/
          retention-days: 14
